#include<iostream>
//#include"HuffmanCoding.h"
//#include"BinarySearchTree_base.h"
//using namespace std;
//#include<vector>
//#include<string>
#include<string>
#include"stack.h"
#include"LoopLinkList.h"
#include"vanEmdeBoas_tree.h"
int main()
{
	/*char c[10] = {'f','e','c','b','d','a','s','j'};
	float freq[10] = {5,9,12,13,16,45,7,8};
	std::vector<std::string> v;
	huffmanCoding(c, freq, 6,v);
	for (int i = 0; i != v.size(); ++i) {
		std::cout << v[i] << std::endl;
	}*/
	size_t f = -1;
		/*LinkList   lis;
		for (int i = 0; i != 10; ++i) {
			std::string s(i, 's');
			lis.insert(s);
		}

		for (int i = 0; i != 5; ++i) {
			std::string s(i, 's');
			lis.pop(s);
			lis.insert(s);
		}
		std::string t = lis.find("s");
		
	
	std::cout << lis.size();*/
	std::cout << f;
	system("pause");
	return 0;
}